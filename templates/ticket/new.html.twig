{% extends 'baseadmin.html.twig' %}

{% block title %}Créer un nouveau ticket{% endblock %}

{% block body %}
<section id="newticket">
    <div class="container position-relative">
        <div class="row gy-5" data-aos="fade-in">
            <div class="col-lg-12  order-lg-1 d-flex flex-column  caption">
    <h1>Créer un nouveau ticket</h1>
                <div class="form-group">
                <button class="btn btn-success">{{ button_label|default('Valider') }}</button>
                <a href="{{ path('app_ticket_index') }}" class="btn btn-dark">Retour</a>
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}
