<form method="post" action="{{ path('app_time_tracking_delete', {'id': time_tracking.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ time_tracking.id) }}">
    <div class="d-grid gap-2 pt-2">
        <button class="btn btn-block btn-danger">Effacer</button>
    </div>
</form>
